function countdown(n){
    if (n < 1) {
    return [];
  } else {
    const countArray = countdown(n - 1);
    countArray.unshift(n); //unhift places items at the front of the array
    return countArray;
  return; n }
}
console.log(countdown(10)); // [5, 4, 3, 2, 1]
