//Change an if-else statement to a Conditional (Ternary) Operator or one line if-else expression

function checkEqual(a, b) {
    a == b ? console.log ("Equal") : console.log ("Not Equal");
    return a == b ? "Equal" : "Not Equal"; //i could use any inequality = < >
}

checkEqual(1, 1);
