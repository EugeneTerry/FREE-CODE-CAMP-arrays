function convertToInteger(str) {
console.log(parseInt(str)); //The parseInt() function parses a string and returns an integer
return parseInt(str);
}
convertToInteger("001"); // this will return a "1" as an integer

let numNum = 23.556; 
console.log(numNum.toFixed(0)); // this rounds the number to the nearest whole number because of the 0 means 0 decimals
