// example: parseInt(string, radix);
// so this will covert binary numbers to intergers
function convertToInteger(str) {
    console.log (parseInt(str, 2))
return parseInt(str, 2); 
//The radix variable "2" says that "0110" is in the binary system, or base 2.
//This example converts the string "0110" to an integer 6.
}

convertToInteger("0110"); 
